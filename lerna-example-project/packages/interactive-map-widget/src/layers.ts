/* stylelint-disable */
import { LayerDefinition, LayerTypes, Preset } from "@dtn/map-lib";

export const layers: ReadonlyArray<LayerDefinition> = [];

export const presets: ReadonlyArray<Preset> = [];
