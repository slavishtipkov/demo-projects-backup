# @dtn/stylelint-config

Shared stylelint configuration.

## Install

```sh
npm install --dev @dtn/stylelint-config stylelint
```

## Usage

Add a `.stylelintrc` file to the root of your project.

```json
{
  "extends": ["@dtn/stylelint-config"]
}
```
